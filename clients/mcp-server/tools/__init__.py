"""Tertiary-analysis tools (phase 2): assembled tables -> insight.

One module per provider/domain: `microbiome` (the toolkit's own in-process Python
analyses), `biobakery` (the external bioBakery stats suite, run through the R backend),
`spatial` (spatial transcriptomics).
"""
